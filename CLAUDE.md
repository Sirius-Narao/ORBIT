# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What ORBIT is

ORBIT (Open Research & Benchmarking Intelligence Toolkit) is a **terminal-first AI/research toolkit**, not a PyTorch clone. Core philosophy: *the user specifies the experiment → ORBIT handles the machinery → ORBIT records the results → the user analyzes and reproduces them.*

**Hard architectural rule: experiments are declarative saved configs, not Python scripts.** The user never hand-writes a `.py` file that builds a `Model`/`Optimizer`/`Trainer` and calls `.run()` — that's ORBIT's own internal engine code (what you're writing in `src/orbit/`), not user-facing. `orbit new` interactively walks the user through configuring dataset/model/optimizer/hyperparams and saves the result as a JSON config; `orbit run <experiment>` loads that saved config and is what actually constructs the `Tensor`/`Model`/`Loss`/`Optimizer`/`Trainer` objects and executes training. Flow: `experiment.json` → ORBIT engine (`core`/`nn`) → results.

On-disk layout (per experiment):
```
.orbits/
└── experiments/
    └── <name>/
        ├── experiment.json   # the saved config
        ├── results/
        └── checkpoints/
```
Example `experiment.json`, corrected schema (resolves the two gaps in the original draft: `loss` is now an explicit field, and the first layer explicitly carries `in_features` since nothing else in the schema states input dimensionality):
```json
{
  "name": "xor_mlp_01",
  "dataset": "xor",
  "model": [
    {"type": "Linear", "in_features": 2, "neurons": 8},
    {"type": "Tanh"},
    {"type": "Linear", "neurons": 1},
    {"type": "Sigmoid"}
  ],
  "loss": "MSE",
  "optimizer": "SGD",
  "learning_rate": 2.0,
  "batch_size": 4,
  "epochs": 3000,
  "seed": 42
}
```
(`mnist`/`Adam` from the original draft don't exist in ORBIT yet — see the config-loader v1 scope note below. `batch_size` defaults to `DataLoader`'s own default, `32`, if omitted. `seed` is optional — omit it for an unseeded/non-reproducible run; when present, `load_experiment` calls `np.random.seed(seed)` before building anything, since `Linear`'s weight init and `DataLoader`'s shuffling both draw from the global NumPy RNG, so this one call fixes both for the whole run. `orbit new`/`orbit copy` both auto-fill a random seed when the user leaves the prompt blank, so an experiment saved through the CLI always has one; only a hand-written or hand-edited `experiment.json` would omit it.) Only the first layer in `model` needs `in_features`; every layer after it infers its input width from the previous layer's `neurons`. An optional `"task"` field (`"binary_classification"` or `"multiclass_classification"`, see `TASK_REGISTRY` below) turns on per-epoch accuracy tracking; omit it and only loss (plus gradient norm — see `_gradient_norm` below) is tracked, matching every config written before accuracy tracking existed. An optional `"test_split"` field (float, e.g. `0.2`) holds out a fraction of the dataset for a post-training evaluation pass — see "Planned: normalization, train/test split..." below for the implemented details (`core/dataset.py`'s `train_test_split`/`Subset`, `Results.test_loss`/`test_accuracy`); omit it for training-only behavior, unchanged from before this field existed. An optional `"normalize"` field (`"standard"` or `"minmax"`; absent or `"none"` = off) rescales the **inputs only** with stats fit on the training split — see that same section's item #1. The additional regression task types described there are designed but **not yet implemented**.

**Full command surface** (`init`, `new`, `run <experiment>`, `train`, `test`, `list`, `inspect`, `delete`, `compare`, `reproduce`, `copy`, `rename`, `import`, `plotloss`, `plot`, and the bare `orbit` REPL are now implemented end-to-end — parser → command → storage/config loader → engine; the "configure then run" half of `run`, `new --template`, and `sweep *` are still stub):
- `orbit` — opens the interactive ORBIT research environment. Implemented in `cli/repl.py` (`repl()`): prints an ASCII banner, then loops reading lines with `questionary.text(...).unsafe_ask()`, `shlex.split`s each into tokens, and feeds them through the *same* `build_parser()`/`_dispatch()` as the direct CLI — so every subcommand below works identically inside the REPL. `help`/`?` prints `parser.format_help()` plus the REPL-only meta-commands; `exit`/`quit` (or Ctrl-D) leaves; Ctrl-C clears the current line instead of exiting; a `SystemExit` from a bad `argparse` line or any other exception from a command is caught so one bad line can't kill the session. See `tests/cli/test_repl.py`.
- `orbit init` — initializes a new ORBIT project. Implemented in `cli/commands/init.py` (`init_project`): `mkdir(parents=True, exist_ok=True)`s both `.orbits/experiments/` and `.orbits/datasets/`; idempotent and non-destructive, so re-running it on an already-initialized project just backfills whichever directory is missing rather than erroring, printing a warning ("already initialized") instead of the success message in that case.
- `orbit import <csv-path> [--name NAME] [--target COL [COL ...]]` — imports a CSV as a named dataset under `.orbits/datasets/<name>/` (a copy of the CSV plus a `dataset.json` manifest of input/output column names). Implemented in `cli/commands/import_dataset.py`; see "Dataset design reassessment" below for the design and remaining scope limits (CSV + numeric columns only).
- `orbit run` — starts an experiment setup flow (configure dataset/model/optimizer/training settings), then runs it. *(Distinct from `orbit run <experiment>` reproducing a saved config — the same verb covers both "configure then run" and "load then run.")* Only the `orbit run <experiment>` (load-a-saved-config) half exists so far, in `cli/commands/run.py`; the "configure then run" half is still stub. `orbit run <experiment>` trains a full run and reports `results.final_loss`, warning if the run looks stalled (see `_warn_if_stalled` in `cli/parser.py`); when the config sets `"test_split"` it also reports `results.test_loss`/`results.test_accuracy` right after (see "Planned: normalization, train/test split..." below for how that split/evaluation works). **Confirmed decision: `orbit run` keeps this exact behavior (train, then evaluate on the test split when one is configured, in one call) — it is not being renamed or split apart.** See the planned `orbit train`/`orbit test` commands below for the narrower alternatives.
- `orbit new` / `orbit new --template <name>` — creates a new experiment interactively (optionally from a template), still asking the user to configure it. The base flow (no `--template`) is implemented in `cli/commands/new.py`; `--template` is not. The base flow also lets the user opt into per-epoch accuracy tracking (`"Track accuracy?"`, backed by `TASK_REGISTRY` — see the config-loader scope note below), pick input normalization (`"Normalize inputs?"` select: `none`/`standard`/`minmax`, written only when not `none`), and always auto-fills a random `seed` if left blank.
- `orbit list` — lists previously recorded experiments. Implemented in `cli/commands/list.py` (`list_experiments`): scans `.orbits/experiments/`, rendering a `rich.table.Table` with one row per experiment directory — name, "done"/"not run" status (based on whether `results/results.json` exists), final loss, and epoch count. Prints a warning instead of a table when the root doesn't exist or is empty.
- `orbit inspect <id>` — shows an experiment's configuration, metrics, recorded info. Implemented in `cli/commands/inspect.py` (`inspect_experiment`): loads `experiment.json` and reuses `new.py`'s `_print_config_summary` table to show it, then prints final loss/epoch count from `results.json` if the experiment has been run, or "Not run yet." if not.
- `orbit compare <id> <id> ...` / `orbit compare --all [--plotloss] [--logscale]` — compares experiments via metrics/stats/visualizations. Implemented in `cli/commands/compare.py` (`compare_experiments`): one `rich.table.Table` row per experiment with dataset/loss/optimizer/learning rate/seed/batch size/epochs/final loss, skipping (with a warning) any name that isn't found; `--all` compares every experiment under `.orbits/experiments/` instead of a named list. `--plotloss` additionally overlays every compared experiment's loss curve on one `matplotlib` plot (`visualization.loss.plot_loss_comparison`), saved under `.orbits/comparisons/` (filename is the sorted, `_vs_`-joined experiment names, falling back to `comparison_<n>_experiments.png` once that would exceed 100 characters); `--logscale` renders that comparison plot's y-axis on a log scale (same flag name/meaning as `orbit plotloss`'s, see below). Flag naming follows this codebase's established convention: name a plot flag after the metric (`--plotloss`), never a generic `--plot`.
- `orbit reproduce <id>` — re-runs an experiment from its recorded configuration. Implemented in `cli/commands/reproduce.py` (`reproduce_experiment`): warns up front if the saved config has no `seed` (re-running then isn't expected to match), calls `run_experiment` again, and compares the new `final_loss` to the previously recorded one (`abs(...) < 1e-9`) to report a match or a diff.
- `orbit copy <id>` — creates a new experiment using an existing one's config as defaults, editable. Implemented in `cli/commands/copy.py` (`copy_experiment`): dataset/model/loss/optimizer/task are copied unchanged from the source (changing those means running `orbit new` from scratch); `normalize`/`learning_rate`/`batch_size`/`epochs`/`test_split`/`seed` are re-prompted with the source's values pre-filled as defaults (`normalize` via a `questionary.select` with `default=` the source's value — flipping only normalization is a one-keystroke ablation) — keeping the source's seed (just hit enter) isolates the effect of a hyperparameter change from random-init/shuffle noise, the same way an ablation study holds everything but one variable fixed; clearing the seed field picks a fresh random one for a genuinely independent run.
- `orbit rename <old_name> <new_name>` — **not in the original command-surface draft**, added alongside `delete`/`copy`/`compare`/`list`/`inspect`. Implemented in `cli/commands/rename.py` (`rename_experiment`): renames the experiment's directory, then rewrites the `"name"` field inside both `experiment.json` and `results.json` (if present) so they stay in sync with the directory `list`/`inspect`/etc. look them up under. Refuses if `old_name`/`new_name` are identical, `old_name` doesn't exist, or `new_name` is already taken.
- `orbit delete <name> [--all]` — **not in the original command-surface draft**, added alongside `rename`/`copy`/`compare`/`list`/`inspect`. Implemented in `cli/commands/delete.py` (`delete_experiments`): `shutil.rmtree`s a named experiment's directory, or every experiment directory under `.orbits/experiments/` when `--all` is passed instead of a name. No confirmation prompt — this is a destructive, irreversible operation exposed directly on the CLI, so treat it with the same care as any other `rm -rf`-shaped command when suggesting or scripting its use.
- `orbit plotloss <id> [--logscale]` — **renamed from `orbit plot`** (see the new `orbit plot` bullet immediately below for why). Unchanged behavior otherwise: loss-only, single-experiment. `cli/commands/plotloss.py` (`plot_experiment`) mirrors `inspect_experiment`'s two-guard shape (missing experiment → warn; no `results.json` yet → warn "has not been run yet - nothing to plot (run it first)."), then calls `visualization.loss.plot_loss(results, exp_dir/"results"/f"loss{extension}.png", log_scale=log_scale)` (`extension` is `"_log_scale"` when `--logscale` is passed, so a log-scale and linear plot of the same experiment don't clobber each other) and reports the saved path via `success(...)`. `plot_loss` renders a `matplotlib` line plot (epoch on x, loss on y, ORBIT's `#b0ffb3` accent color) and saves it as a real PNG. `matplotlib` is imported lazily *inside* `plot_loss`/`plot_loss_comparison` and every other `visualization/*.py` plot function, not at module scope, since `cli/parser.py` imports every `cli/commands/*.py` module unconditionally at startup and a top-level import would tax every `orbit` invocation with matplotlib's load cost; the `Agg` (non-interactive) backend is forced since this only ever saves to disk — there's no `--show`/interactive-window flag, deliberately, since `cli/repl.py` keeps one process alive across many commands in a session and backend-switching mid-process is a footgun not worth taking on (`plt.close(fig)` after each save keeps the REPL's long-lived process from leaking figures across repeated calls). See `tests/visualization/test_loss.py`, `tests/cli/test_plotloss.py`.
- `orbit plot [name ...] [--all] [--logscale] [--metrics ...]` — **implemented**, and a *different* command from `orbit plotloss` above: interactively plots one or more experiments' metrics, added per explicit user request superseding the originally-sketched multi-metric design (see "Planned: normalization, train/test split..." item #5 below). `cli/commands/plot.py` (`plot_experiments`): resolves `names`/`--all` the same way `compare_experiments` does (warn + `[]` on no experiments found), then gathers `(name, exp_dir, Results)` for every requested name that exists and has been run, warning+skipping ones that don't (mirrors `compare_experiments`' `plot_candidates` pattern). Which metric(s) to plot — Training loss, Accuracy, Gradient norm, Test loss, Test accuracy — is chosen via a `questionary.checkbox` prompt (same pattern as `import_dataset.py`'s target-column prompt) unless `--metrics` (internal keys: `loss`/`accuracy`/`gradient_norm`/`test_loss`/`test_accuracy`, enforced by argparse's `choices=`) is given to bypass it, e.g. for scripting. Training loss/accuracy/gradient norm plot as line curves (`visualization.loss`/`accuracy`/`gradients`, single line or overlaid-with-legend depending on experiment count); test loss/test accuracy — single scalars per experiment, not per-epoch histories, so they can't be line-plotted the same way — render as **bar charts** instead (new `visualization/test_metrics.py`, one bar per experiment, via a shared `_bar_chart` helper; unlike the line metrics there's no separate single/comparison function since one bar generalizes to N with no code-shape difference). Each metric is filtered to only the experiments that actually have it (an experiment missing `"task"` or never `orbit test`-ed is skipped **for that metric only**, with a `warning()`, not a hard failure) — this is deliberate: experiments in one `orbit plot` call can be heterogeneous (different configs tracking different things). Whether output goes to a single experiment's own `results/` dir (`<exp>/results/<metric>.png`) or to `.orbits/comparisons/` (reusing `compare.py`'s `_comparison_filename`, now extended with an optional `metric=` parameter — default `None` keeps `compare --plotloss`'s existing filenames byte-identical, `plot` passes e.g. `metric="loss"` to get `exp_a_vs_exp_b_loss_log_scale.png`) is decided **once per invocation**, from how many requested experiments actually had *any* results — not re-decided per metric — so a call naming 2 experiments where only 1 turns out to have `test_accuracy` still writes under `.orbits/comparisons/` (just named only for the experiment(s) actually in that particular plot). See `tests/cli/test_plot.py`, `tests/cli/test_compare.py` (`_comparison_filename`'s new `metric=` tests), `tests/visualization/test_accuracy.py`, `test_gradients.py`, `test_test_metrics.py`.
- `orbit train <experiment>` / `orbit test <experiment>` — **implemented.** `train` (`cli/commands/train.py`, `train_experiment`) mirrors `run_experiment` exactly except it calls `experiment.run(skip_test=True)` (a new optional parameter on `Experiment.run`, default `False`, preserving `orbit run`'s existing behavior untouched) so no test-set evaluation happens even when `"test_split"` is configured. Both `orbit run` and `orbit train` now also save a checkpoint of the trained model's weights (`storage/artifacts.py`'s new `save_checkpoint`, see below) right after `save_results`, since `orbit test` needs one regardless of which command produced it. `test` (`cli/commands/test.py`, `test_experiment`) evaluates an **already-trained** experiment on demand, without retraining: it loads `experiment.json` with the exact same try/except-raise-`FileNotFoundError` pattern `run_experiment`/`train_experiment` use (so a nonexistent experiment fails identically across all three), then warns and returns `None` if no checkpoint has been saved yet (`storage/artifacts.py`'s `checkpoint_exists`) or if the config has no `"test_split"` field (nothing to evaluate against); if the config has no `"seed"` it warns (mirroring `orbit reproduce`'s equivalent warning) but still proceeds. On success it calls `load_experiment(config)` again to reconstruct the identical dataset/train-test split (same seeded-RNG-replay convention `orbit reproduce` already relies on), overwrites the freshly-initialized model's weights via `load_checkpoint`, calls `Trainer.evaluate(...)` directly, and persists the resulting `test_loss`/`test_accuracy` back into `results.json` (`load_results`/`save_results`) so a later `orbit inspect` reflects the fresh evaluation. See `tests/cli/test_train.py`, `test_test.py`, `tests/storage/test_artifacts.py`, and the added cases in `tests/core/test_experiment.py`/`tests/cli/test_parser.py`/`test_run.py`.
- `orbit sweep create/start/status/resume/compare/plot/export/reproduce` — hyperparameter sweep lifecycle: define a search space, launch all configs, check progress, resume an interrupted sweep without rerunning completed configs, analyze/compare/plot across configs, export (e.g. CSV), or recreate an entire past sweep.

Note: `experiments/xor/experiment.py` and `experiments/mnist/experiment.py` (currently empty) predate this corrected architecture and were scaffolded assuming a script-based model — they're likely the wrong shape now (should probably become `.orbits/experiments/<name>/experiment.json`-style configs instead, or get removed). Don't fill them in as `.py` scripts without checking first.

**Current reality**: the core tensor/autograd engine, the NN building blocks (`Module`, `Parameter`, `Linear`, `Sequential`, activations, losses), a minibatch data pipeline (`Dataset`/`DataLoader`), a batched training loop (`Optimizer`/`SGD`, `Trainer`), and an `Experiment`/`Results` orchestration layer all exist and are tested end-to-end (see `tests/integration/test_xor.py` for the full Tensor → Module → Linear → Activation → Loss → autograd → Optimizer round trip, and `tests/core/test_experiment.py` for the `Experiment`/`Results` layer on top of it). On top of that, **the config loader (`core/config.py`) is now implemented** — `load_experiment(config_dict)` builds a real `Dataset`/`Sequential`/`Loss`/`SGD`/`Experiment` from an `experiment.json`-shaped dict via `DATASET_REGISTRY`/`LAYER_REGISTRY`/`LOSS_REGISTRY`/`OPTIMIZER_REGISTRY`/`TASK_REGISTRY` (plus the optional `seed` handling described above) — and the full experiment-lifecycle slice of `cli/` is implemented on top of it: `cli/commands/init.py` (`init_project`), `new.py` (`create_experiment`), `run.py` (`run_experiment`), `train.py` (`train_experiment`), `test.py` (`test_experiment`), `list.py`, `inspect.py`, `delete.py`, `compare.py`, `reproduce.py`, `copy.py`, `rename.py`, `plotloss.py`, `plot.py` (`plot_experiments`), and `import_dataset.py`, all wired together by `cli/parser.py`'s `argparse`-based `build_parser()`/`_dispatch()`, which `cli/repl.py` also drives for the bare `orbit` REPL. See `tests/cli/test_new.py`, `test_parser.py`, `test_run.py`, `test_train.py`, `test_test.py`, `test_list.py`, `test_inspect.py`, `test_delete.py`, `test_compare.py`, `test_reproduce.py`, `test_copy.py`, `test_rename.py`, `test_repl.py`, `test_plotloss.py`, `test_plot.py`, and `test_import.py`. `Trainer`/`Experiment`/`Results` also now track per-epoch gradient norm (always) and per-epoch accuracy (when the config sets a `"task"`, via `TASK_REGISTRY`/`build_accuracy_fn`) and total run duration — see the training-layer and experiment-orchestration sections below. Storage now has three implemented modules — `storage/experiments.py` (`save_results`/`load_results`/`experiment_dir`/`EXPERIMENTS_ROOT`), `storage/datasets.py` (imported-dataset manifests, see "Dataset design reassessment" below), and `storage/artifacts.py` (`save_checkpoint`/`load_checkpoint`/`checkpoint_exists`/`checkpoint_path`, a minimal `.npz`-per-experiment round-trip of a `Module`'s `named_parameters()`, backing `orbit train`/`orbit test` — see the command surface above) — `visualization/` now has four implemented modules, `loss.py`, `accuracy.py`, `gradients.py` (all `plot_<metric>`/`plot_<metric>_comparison` line-plot pairs, same shape), and `test_metrics.py` (`plot_test_loss`/`plot_test_accuracy`, bar charts — see the Visualization architecture section below) — only `weights.py`/`terminal.py` remain stub. Everything else (`storage/database.py`, `sweeps/`, `research/`, `simulation/`) is still an empty stub file (0 bytes) sketching the intended architecture — do not assume any of it is implemented, and do not build out large swaths of it speculatively. Implement only what the current task needs, matching the style of the existing engine code.

**Roadmap**: the original 5-step engine plan — (1) `Dataset`/`DataLoader`, (2) batch `Trainer.fit` off a `DataLoader`, (3) a `Sequential` container in `nn/model.py`, (4) plain metric functions in `core/metrics.py`, (5) `Experiment`/`Results` objects bundling model+data+hyperparams+history — is done, plus a first `storage/` layer (`storage/experiments.py`: `save_results`/`load_results`, JSON round-trip of a `Results`), a reproducibility proof (`tests/integration/test_reproducibility.py`: seeding before construction makes a whole run — random init + shuffling — reproducible), and the **config-loader v1** described below, plus `new`/`run` wired to it through `cli/parser.py`. That closed out the milestone the config loader was blocking. The phase that followed was **not** more CLI surface area — it was revisiting the dataset story that `new`/`run` exposed as too thin (see "Dataset design reassessment" below, now fully closed out) plus a UI pass. With that done, a later round of work (commits `90022c3`/`00b5a1c`) went on to add the remaining lifecycle commands anyway — `list`/`inspect`/`delete`/`compare`/`reproduce`/`copy`/`rename`, the bare `orbit` REPL, and a per-experiment `seed` hyperparameter for reproducible runs — plus a softer color palette in `ui.py`. `orbit plot` (loss-curve-only v1, backed by a new `visualization/loss.py`) followed that. A further round (commits `4f3b662`/`b53a1f1`/`2de1403`/`9bb8ce6`) added, in order: `--logscale` on `orbit plot` plus `orbit compare --plotloss`/`--logscale` (multi-experiment loss overlay via the new `plot_loss_comparison`); wall-clock `duration_seconds` tracking on `Trainer`/`Results`; per-epoch `gradient_norm_history` tracking (`Trainer._gradient_norm`); and, most recently, per-epoch `accuracy_history` tracking gated by a config's `"task"` field, via the new `TASK_REGISTRY`/`build_accuracy_fn` in `core/config.py`. `orbit init` also now exists (`cli/commands/init.py`). A train/test split (`"test_split"`), `orbit train`/`orbit test`, and minimal model checkpointing (`storage/artifacts.py`) were added next — see the command surface above and "Planned: normalization, train/test split..." below for the full design history. Most recently, per an explicit user request left as a note on that commit, the original `orbit plot` was renamed to `orbit plotloss` (unchanged behavior) and a new `orbit plot [name ...] [--all] [--metrics ...]` was added — interactive multi-metric, multi-experiment plotting (loss/accuracy/gradient norm as line plots, test loss/test accuracy as bar charts via the new `visualization/test_metrics.py`) — which completes item #5 below with a different (superseding) design than originally sketched. After that, input normalization (item #1 below: the `"normalize"` config field, fit-on-train-only) was added. All of that is implemented; see the command surface above for specifics. The remaining unimplemented pieces from the *original* roadmap are `orbit run`'s "configure then run" half, `orbit new --template`, `orbit sweep *`, and everything under `sweeps/`/`research/`/`simulation/`/`storage/database.py` — treat picking any of these up as its own planning conversation, not a queued task. On top of that original roadmap, a new, explicitly-scoped round of work was planned next — input normalization, a train/test split, `orbit train`/`orbit test` commands, regression accuracy metrics, and multi-metric visualization — see "Planned: normalization, train/test split..." below; #1 (normalization), #2 (split), #3 (`orbit train`/`orbit test`), and #5 (multi-metric visualization) of that section are now done; only #4 (regression accuracy) remains a queued task.

**Config-loader v1 scope** (implemented, and deliberately narrow — covers only what ORBIT already has, not the full example schema above): `optimizer` supports `"SGD"` only (`SGD` is the only `Optimizer` subclass that exists — no `"Adam"` yet); `dataset` resolves through `DATASET_REGISTRY`, a small registry of inline datasets ORBIT already effectively has (currently just `"xor"` → the 4-row XOR data used throughout `tests/`) plus disk-backed imported CSV datasets (see "Dataset design reassessment" below); `model` layer `"type"` supports whatever's implemented in `nn/` via `LAYER_REGISTRY`: `Linear`, `ReLU`, `Tanh`, `Sigmoid`, `Softmax`, via `LOSS_REGISTRY`: `MSE`, `CrossEntropy`; an optional `"task"` field resolves through `TASK_REGISTRY` (`"binary_classification"` → `metrics.accuracy`, `"multiclass_classification"` → `metrics.accuracy_multiclass`) to turn on per-epoch accuracy tracking — omitted, no accuracy is tracked (`Results.accuracy_history` stays `None`). `Adam`, regression task types, and anything else beyond this list are separate, later steps (regression task types specifically are designed in "Planned: normalization, train/test split..." below, not yet implemented).

### Dataset design reassessment (opened by the `be3a0cb` commit note — read before touching `orbit import`, `DATASET_REGISTRY`, or `cli/commands/new.py`'s dataset/shape prompts)

Building `new`/`run` end-to-end against the v1 config loader surfaced three concrete gaps, in the committer's own words, that should shape the next round of design rather than being patched over individually:

1. **Only `"xor"` exists in `DATASET_REGISTRY`; there's no way to bring in real data.** **Done, CSV only.** `DATASET_REGISTRY` itself is untouched (still just the hardcoded `"xor"` factory) — imported datasets are a separate, disk-backed lookup, not merged into that dict. `src/orbit/storage/datasets.py` (new) mirrors `storage/experiments.py`'s shape: `DATASETS_ROOT = .orbits/datasets`, `dataset_dir`/`save_dataset_manifest`/`load_dataset_manifest`/`dataset_exists`/`list_imported_dataset_names`. `orbit import <csv> [--name NAME] [--target COL ...]` (`cli/commands/import_dataset.py`) reads the CSV header, prompts (`questionary.checkbox`) for the target/output column(s) if `--target` isn't given (every other column becomes input automatically), copies the CSV to `.orbits/datasets/<name>/data.csv`, and writes a `dataset.json` manifest of `input_columns`/`output_columns`. `build_dataset` (`core/config.py`) falls back to a new `_load_csv_dataset` (stdlib `csv` module, no new dependency) when a name isn't in `DATASET_REGISTRY` but `dataset_exists` on disk — reuses `TensorDataset` directly, no new `Dataset` subclass. New `list_dataset_names()` (`= list(DATASET_REGISTRY) + list_imported_dataset_names()`) feeds `orbit new`'s dataset picker, so imported datasets appear alongside `"xor"` with no other changes needed there (`build_model`'s shape checks, the stalled-run warning, etc. all already work against any `Dataset`). **Scope, deliberately narrow**: CSV only, numeric columns only (a non-numeric cell raises `ValueError` naming the column) — no categorical encoding, no missing-value handling, no `orbit list`/`orbit inspect` for datasets. Note: `storage/__init__.py` must import `.datasets` *before* `.experiments` — `.experiments` imports `orbit.core`, which imports `orbit.core.config`, which imports these dataset helpers back from `orbit.storage`; the reverse order hits a circular-import `ImportError` since those names wouldn't be bound yet. See `tests/storage/test_datasets.py`, `tests/core/test_config.py` (`test_build_dataset_resolves_an_imported_csv_dataset` etc.), `tests/cli/test_import.py`, and `tests/cli/test_new.py::test_create_experiment_dataset_picker_includes_imported_datasets`.
2. **Datasets are too implicit — ORBIT doesn't know a dataset's input/output feature counts.** **Done, both directions.** `Dataset`/`TensorDataset` (`core/dataset.py`) expose `input_shape`/`output_shape` properties (plain ints — the per-sample feature count, e.g. `self.X.shape[-1]`; the base `Dataset` raises `NotImplementedError`, matching the existing `__len__`/`__getitem__` convention). `build_model` (`core/config.py`) takes an optional `dataset` argument and raises `ValueError` if the first `Linear` layer's declared `in_features` doesn't match `dataset.input_shape`, **and** (symmetrically, checked once the layer loop finishes) if the last `Linear` layer's `neurons` doesn't match `dataset.output_shape` — `in_features` is tracked across the loop and, since activations don't change it, still holds the final output width by the time the loop ends. Neither check auto-fills a missing value — `in_features` is still required explicitly in the config, per the documented `experiment.json` schema (a missing `in_features` is its own, separate `ValueError`, checked before the input-mismatch check). `orbit new` (`cli/commands/new.py`) builds the chosen dataset immediately after the user picks its name, prints a line like `Dataset 'xor': 2 input feature(s), 1 output feature(s)`, passes `dataset.input_shape` into `_ask_model_layers` to fill the first `Linear` layer's `in_features` directly (no prompt for it at all), and — since the *output* width isn't known until the user says "Done" — calls `build_model(layers, dataset)` at that point purely to validate; on `ValueError` it prints the message and loops back to "Add a layer" instead of saving a broken config, letting the user add a corrective layer. See `tests/core/test_dataset.py` (shape properties), `tests/core/test_config.py` (`build_model`'s input- and output-shape tests), and `tests/cli/test_new.py` (`test_create_experiment_fills_in_features_from_dataset_without_prompting`, `test_create_experiment_rejects_output_shape_mismatch_and_lets_user_fix_it`).
3. **CLI output isn't appealing or informative enough.** **Done, pass 1 (training + interactive flows only).** A new top-level `src/orbit/ui.py` (sibling to `cli/`, `core/`, `nn/`, `storage/` — deliberately *not* placed under `cli/`, since `nn/training/trainer.py` needs the same console/tty logic and `nn/` must never depend on `cli/`) is the shared styling module: a `rich.console.Console` instance, `info`/`success`/`warning`/`error` helpers (thin `console.print(msg, style=...)` wrappers), `is_tty()` (checked fresh on every call, never cached, so pytest's `capsys` swap of `sys.stdout` is detected correctly regardless of import order), and a shared `questionary.Style` (`PROMPT_STYLE`) so prompts and `rich` output share one look. `new.py`/`import_dataset.py`/`parser.py` route their prompt/print calls through this module instead of bare `print()`/unstyled `questionary` calls; `new.py` additionally renders a `rich.table.Table` summary of the full experiment config right before saving `experiment.json`. `Trainer.fit()`'s verbose path (see below) got a live `rich.progress.Progress` bar. `rich` is now a dependency (`pyproject.toml`). Scope was deliberately narrow: `list`/`inspect`/`compare`/etc. stayed untouched stub files at the time (nothing to tabulate yet — they were built out later, see below), and no ASCII banner/logo was added yet (also added later, once the bare `orbit` interactive REPL existed). No dedicated tests were added for `ui.py` itself (it's plain print/style wrappers); `tests/cli/test_new.py`/`test_parser.py`'s existing `capsys` substring assertions were re-verified to still pass, since `rich`'s `Console` resolves `sys.stdout` dynamically at print time and auto-suppresses ANSI codes once the stream isn't a real terminal. **Update (`00b5a1c`): the palette was reworked** from `info`/`success`/`warning`/`error`'s original cyan/green/yellow/red style names to a softer custom set of hex colors — `info` and the shared `PROMPT_STYLE` accents use `#ffeab0` (warm gold), `success` uses `#b0ffb3` (soft green), `warning` uses `#d6b0ff` (soft purple — no longer yellow), and `error` uses `#ffb0b0` (soft red) — plus a `rich.theme.Theme` (`THEME`, passed into the module's `Console`) that overrides `rich`'s own internal style names (`bar.back`/`bar.complete`/`bar.finished`/`bar.pulse`, `progress.percentage`/`progress.elapsed`/`progress.remaining`) so `Trainer.fit()`'s progress bar picks up the same palette automatically instead of `rich`'s magenta/pink/yellow/cyan defaults, with no per-column style plumbing needed in `trainer.py`. **Update: fixed a Windows encoding crash in the REPL banner.** `ui.py`'s `Console` is constructed with `legacy_windows=False`, and `sys.stdout`/`sys.stderr` are `.reconfigure(encoding="utf-8", errors="replace")`'d at module import time (wrapped in `try/except (AttributeError, ValueError)` since not every stdout stand-in, e.g. pytest's `capsys`, supports `.reconfigure()`). Without both: whenever stdout isn't attached to a real Windows console (piped through Git Bash/mintty, redirected to a file, wrapped by another process), Windows' PEP 528 UTF-8 console handling doesn't apply, Python falls back to the system locale's codepage (commonly `cp1252`), and any of this module's own non-`cp1252` output — `cli/repl.py`'s box-drawing banner and em dashes chief among them — raised a bare `UnicodeEncodeError` and crashed the whole REPL before a single prompt was shown. Both fixes are necessary: `legacy_windows=False` alone still crashes one layer down (`rich` falls through to a plain `file.write()` that still hits the same `cp1252` codec); the UTF-8 reconfigure alone still crashes inside `rich`'s legacy Win32-console code path. Verified fixed under both a real PowerShell session and Git Bash. (A separate, unrelated `prompt_toolkit` limitation remains: `questionary`'s prompts — and so the REPL, and `orbit new`/`orbit copy` — can't run when stdin/stdout is piped/redirected rather than a live attached terminal, e.g. under a test harness or `orbit < input.txt`; that's inherent to `prompt_toolkit` on Windows, not fixable from `ui.py`.)

All three gaps from this reassessment are now closed, and the CLI-facing work that gap 3's note deferred — the remaining lifecycle commands and the REPL's ASCII banner — is also done (commits `90022c3`/`00b5a1c`; see the command surface above). `orbit plot` (loss-only v1, then `--logscale`), `orbit compare --plotloss`, gradient-norm/duration/accuracy tracking, and `orbit init` have since been implemented too (see the Roadmap paragraph above for the commit-by-commit order). What's left unimplemented from the original draft is `orbit new --template` and `orbit sweep *` — treat picking either of those up as its own planning conversation, not a queued task. A new, separately-scoped round of work was planned next — see immediately below, now mostly done.

### Planned: normalization, train/test split, `orbit train`/`orbit test`, regression accuracy, and multi-metric visualization (everything except regression accuracy now implemented — read before starting #4)

Opened 2026-09-20 by explicit user requirement (not a code-driven gap like the dataset reassessment above): normalize dataset inputs, hold out a test split, add dedicated train/test commands, broaden accuracy beyond classification (e.g. a wine-quality-style regression dataset), and plot metrics beyond loss. Six requirements below, several with a real build-order dependency on each other — read the "Build order" note at the end before picking one up in isolation. #1 (normalization), #2 (train/test split), #3 (`orbit train`/`orbit test`), and #5 (multi-metric visualization) are done; #4 is still planned.

1. **Input normalization.** **Done.** Optional config field `"normalize"`: `"standard"` (z-score, mean 0 / std 1 per feature) or `"minmax"` (per-feature `[0, 1]`); absent or `"none"` = off, so every existing `experiment.json` and test fixture behaves byte-for-byte as before. **Inputs (X) only** — confirmed with the user: targets stay in their original units so losses/test losses stay comparable across normalized and unnormalized runs, and 0/1 classification labels are never touched. `core/dataset.py` gained `NORMALIZE_METHODS`, `fit_normalizer(X, method) -> stats` (per-column stats; a zero std/range — a constant feature — is replaced by 1 so it maps to zeros instead of NaN; unknown method → `ValueError`), `apply_normalizer(X, stats)`, and `NormalizedDataset(Dataset)`, a generic view (like `Subset`) that applies the stats to `x` in `__getitem__` and delegates everything else — deliberately not a flag on `TensorDataset`/`Subset`, which are used unnormalized throughout `tests/`. `load_experiment` (`core/config.py`) fits the stats on the **train split only** right after `train_test_split` (gathering train rows generically via `train_dataset[i][0]`), then wraps both train and test datasets with those same stats — fitting on train+test would leak test-set information. With no `"test_split"`, the whole dataset is the training set. **Stats are never persisted**: `orbit test` rebuilds via `load_experiment(config)`, which replays the seeded split and so re-derives identical stats (proven by `tests/integration/test_normalization.py`, where `orbit test`'s test loss matches `orbit run`'s exactly); unseeded configs already get the "may not reproduce" warning. `orbit new` adds a `"Normalize inputs?"` select, `orbit copy` re-prompts it with the source's value as `default=` (confirmed with the user — easy normalization-on/off ablation), and `_print_config_summary` shows a `Normalize` row (so `orbit inspect` shows it too). Also fixed `TensorDataset.__init__`'s misleading "Normalizing inputs" comment (it's only list/`Tensor` → ndarray coercion). Smoke-tested on a CSV with one feature in `[0, 1]` and one in `[0, 1000]`: unnormalized training stalled flat (the large feature saturates `Tanh`), `standard` reached ~150× lower loss. See `tests/core/test_dataset.py` (hand-computed normalizer checks), `tests/core/test_config.py` (train-only fitting, `"none"` ≡ absent, invalid value raises), `tests/cli/test_new.py`/`test_copy.py`, `tests/integration/test_normalization.py`.
2. **Train/test split.** **Done.** New optional config field `"test_split"` (float, e.g. `0.2`): key absent means no split at all, training on the full dataset exactly as before this field existed (this matters because forcing every dataset through an 80/20 split would make tiny fixtures like the 4-row `"xor"` dataset degenerate — 3 train rows, 1 test row — so opting in has to stay a per-experiment choice, not a new default); key present with `null`/no value defaults to `0.2`; key present with a float uses that ratio. `core/dataset.py` gained `Subset(Dataset)` (a generic index-based view over any `Dataset`, not array-based, so it works for CSV-imported datasets too) and `train_test_split(dataset, test_split) -> (Subset, Subset)`, which draws from the global `np.random` RNG (`np.random.permutation`) the same convention `DataLoader`'s shuffle already uses, rounding (not truncating) the test-row count so a 4-row dataset at the default 0.2 gets a non-empty 3/1 split, and raising `ValueError` if either resulting split would be empty. `load_experiment` (`core/config.py`) splits right after building the dataset and before building the model — same seeded RNG stream `load_experiment` already seeds via `"seed"`, so re-running a seeded experiment (`orbit reproduce`) reconstructs the identical split rather than needing row indices persisted separately; an unseeded config with `"test_split"` set gets the same "not expected to reproduce" caveat `orbit reproduce` already warns about for training. `nn/training/trainer.py` gained `Trainer.evaluate(model, loss_fn, dataloader, accuracy_fn=None) -> (loss, accuracy)` (forward pass only — no `zero_grad()`/`backward()`/`optimizer.step()` — brackets the pass in `model.eval()`/`model.train()`); `Experiment` (`core/experiment.py`) takes an optional `test_dataloader` and, when given one, calls `trainer.evaluate(...)` right after `trainer.fit(...)` finishes, folding the result into two new `Results` fields, `test_loss`/`test_accuracy` (both `Optional[float]`, default `None`, round-tripped by `to_dict`/`from_dict` same as the other optional fields). `orbit run`'s CLI output (`cli/parser.py`) and `orbit inspect` (`cli/commands/inspect.py`) both print the test loss/accuracy when present, and `orbit new`/`orbit copy` (`cli/commands/new.py`/`copy.py`) prompt for `"Test split fraction (0-1, blank = no split)"` alongside the other hyperparameters (blank omits the key entirely, matching the "absent = no split" config semantics; `copy.py` pre-fills the source experiment's value as the default, same as its other hyperparameter prompts). Every config without a `"test_split"` key is unaffected — `test_dataloader` stays `None`, `Results.test_loss`/`test_accuracy` stay `None`, byte-for-byte the same behavior as before this field existed. See `tests/core/test_dataset.py` (`Subset`/`train_test_split`), `tests/core/test_config.py` (`load_experiment` with `"test_split"` set, including the null-defaults-to-0.2 case), `tests/nn/test_trainer.py` (`evaluate()`), `tests/core/test_experiment.py`, `tests/core/test_results.py`, `tests/integration/test_reproducibility.py` (seeded split reproducibility), and `tests/cli/test_new.py`/`test_copy.py`/`test_parser.py`/`test_inspect.py` for the CLI-facing pieces.
3. **`orbit train` / `orbit test` commands — `orbit run` stays exactly as it is.** **Done.** `orbit run <experiment>` was **not** renamed and **not** split apart, per the confirmed decision — it still loads a config, trains, and evaluates on the test split (when `"test_split"` is set), all in one call. Two new commands were added alongside it:
   - `orbit train <experiment>` (`cli/commands/train.py`) — training only, no test-set evaluation; for fast iteration while tuning hyperparameters. Calls the new `Experiment.run(skip_test=True)` (additive parameter, default `False`, so `orbit run`'s own `experiment.run()` call is untouched) instead of duplicating `Results`-building logic.
   - `orbit test <experiment>` (`cli/commands/test.py`) — evaluates an **already-trained** experiment's saved model against its held-out test split, on demand, without re-training. Guards (each prints a warning and returns `None`): experiment not found; no checkpoint saved yet; config has no `"test_split"`. If the config has no `"seed"` it warns (same "may not reproduce" caveat as `orbit reproduce`) but still proceeds. On success it calls `load_experiment(config)` again — reconstructing the identical seeded train/test split — loads the checkpoint's weights into the freshly-built model (overwriting its throwaway random init), calls `Trainer.evaluate(...)` directly, and persists `test_loss`/`test_accuracy` back into `results.json` so `orbit inspect` reflects the fresh number.

   `orbit test`'s prerequisite — **model checkpointing**, since `Results` never recorded trained weights and `storage/artifacts.py` was a 0-byte stub — is now built, minimally as scoped: `storage/artifacts.py` gained `save_checkpoint(name, model, root)`/`load_checkpoint(name, model, root)`/`checkpoint_exists(name, root)`/`checkpoint_path(name, root)`, saving one `.npz` per experiment at `.orbits/experiments/<name>/checkpoints/model.npz`, keyed by each parameter's `Module.named_parameters()` name (e.g. `"0.weight"`, `"2.bias"`) rather than positional order — the architecture itself is never re-saved, since `experiment.json`'s `"model"` list plus `build_model` already reconstructs it byte-for-byte before a checkpoint is loaded back in. Both `orbit run` and `orbit train` call `save_checkpoint` right after `save_results`, so `orbit test` works no matter which command produced the trained weights. `nn/training/trainer.py`'s `Trainer.evaluate(...)` (built for #2's post-training test-set pass) is reused as-is by `orbit test`, accessed via `experiment.trainer.evaluate(...)` on the freshly reconstructed `Experiment`. See `tests/storage/test_artifacts.py`, `tests/cli/test_train.py`, `tests/cli/test_test.py`, and the added cases in `tests/core/test_experiment.py` (`run(skip_test=True)`), `tests/cli/test_parser.py` (dispatch), and `tests/cli/test_run.py` (checkpoint side effect).
4. **Regression accuracy metrics.** `TASK_REGISTRY` (`core/config.py`) currently has two entries, both classification (`"binary_classification"` → `metrics.accuracy`, `"multiclass_classification"` → `metrics.accuracy_multiclass`), both exact-match comparisons with no notion of "close enough" for a continuous target. Decision: add **two** new task types for continuous-target datasets (e.g. an imported wine-quality CSV), rather than one metric trying to cover both uses:
   - `"regression_tolerance"` — a prediction counts as correct if it's within a configurable tolerance of the true value. `TASK_REGISTRY`'s entries are currently zero-extra-arg `(y_pred, y_true) -> float` functions (see `build_accuracy_fn`), so a tolerance needs a place to live — most likely a second optional config field (e.g. `"accuracy_tolerance"`) read alongside `"task"` in `build_accuracy_fn`/`load_experiment`, rather than baking one fixed tolerance into the function itself.
   - `"regression_r2"` — coefficient of determination, the conventional regression goodness-of-fit figure. Not a 0–1 "accuracy" in the classification sense; report and (per #5) plot it as its own thing rather than conflating it with the tolerance-based metric.

   Both belong in `core/metrics.py` alongside `accuracy`/`accuracy_multiclass` (same plain-function style, no classes), then registered in `TASK_REGISTRY`.
5. **Multi-metric visualization.** **Done, with a different design than originally sketched below** (superseded by an explicit user request left as a note on the commit that added `orbit train`/`orbit test`, once that request was implemented — see the command surface above for the full `orbit plot`/`orbit plotloss` description). The design *originally* sketched here — per-metric CLI flags on `orbit plot` (`--accuracy`, `--gradnorm`) and on `orbit compare` (`--plotaccuracy`/`--plotgradnorm` siblings to `--plotloss`) — was **not** built; instead, the existing `orbit plot <experiment> [--logscale]` was renamed to `orbit plotloss` (unchanged behavior) and a new `orbit plot [name ...] [--all] [--metrics ...]` was added that **interactively** asks which metric(s) to plot (`questionary.checkbox`, matching `import_dataset.py`'s prompt pattern) rather than one flag per metric, and accepts as many experiment names as needed (or `--all`) in one call rather than requiring `compare` for multi-experiment overlays. The "name a plot flag after the metric, never a generic `--plot`" convention noted below was about `compare`'s per-metric *flags* specifically (`--plotloss`) — it doesn't conflict with a `plot` *command* that itself selects metrics interactively; `orbit compare --plotloss` itself is untouched. `visualization/accuracy.py`/`gradients.py` were built matching `loss.py`'s exact shape (`plot_<metric>`/`plot_<metric>_comparison`, line plots). `test_loss`/`test_accuracy` (scalar per experiment, not per-epoch histories, so no line to plot) got their own new module, `visualization/test_metrics.py` (`plot_test_loss`/`plot_test_accuracy`), rendering **bar charts** instead — one bar per experiment via a shared `_bar_chart` helper, no separate single/comparison split needed since a bar chart already generalizes from 1 to N experiments. `weights.py`/`terminal.py` remain out of scope, unchanged from the original note — nothing tracks weight history yet, and `terminal.py`'s ASCII/terminal-rendered-plot purpose is a different, still-unstarted feature. Regression-metric plotting (once #4 exists) should reuse this same interactive-checkbox `orbit plot`, adding the new task's metric to `_METRIC_CHOICES`/`_LINE_METRICS` or `_BAR_METRICS` in `cli/commands/plot.py` as appropriate, not a new flag-based command.

**Build order**: #1, #2, #3, and #5 are done. #4 (regression accuracy) is the next thing to pick up here — it's independent of the others, and its accuracy plotting (via #5's now-built `orbit plot`) only has something to plot once the new regression task type is actually being tracked on a given experiment. It pairs naturally with #1: a regression CSV like wine quality should usually also set `"normalize"`.

## Commands

Package is installed in editable mode in `.venv` (Windows venv at `.venv/Scripts/python.exe`).

```powershell
# Run the full test suite
.venv\Scripts\python.exe -m pytest

# Verbose
.venv\Scripts\python.exe -m pytest -v

# Run a single test file
.venv\Scripts\python.exe -m pytest tests/core/test_autograd.py -v

# Run a single test
.venv\Scripts\python.exe -m pytest tests/core/test_autograd.py::test_matrix_multiplication_backward -v

# Reinstall the package after dependency/packaging changes
.venv\Scripts\python.exe -m pip install -e ".[dev]"
```

There is no lint/format tooling configured yet (no ruff/black/flake8 config present) and no `pytest.ini`/`conftest.py` — pytest runs on defaults, discovering `tests/`.

## Architecture

### The autograd engine (`src/orbit/core/`)

This is the heart of the project and the most mathematically load-bearing code.

- **`tensor.py`** — `Tensor` wraps a NumPy array plus graph metadata (`operation`, `parents`, `requires_grad`, `grad`). Every operator (`__add__`, `__mul__`, `__matmul__`, `.relu()`, `.softmax()`, etc.) builds a new output `Tensor` and attaches a closure to `out._backward` that knows how to route gradients to that op's specific parents. `.backward()` just delegates to `autograd.backward()`.
- **`autograd.py`** — the actual differentiation engine, deliberately kept simple and heavily comment-documented (treat the comments as intentional teaching material, not clutter to strip out). Key pieces:
  - `build_topological_order(root)` — DFS over `parents` to produce a topological ordering of the graph.
  - `backward(root, gradient=None)` — seeds `root.grad` (defaults to `1` for a scalar) and walks the topological order in reverse, calling each node's `_backward`.
  - `accumulate_gradient(tensor, gradient)` — adds (not overwrites) into `tensor.grad`, since a tensor may be reused in multiple graph paths (e.g. `y = x * x`).
  - One `backward_<op>` function per operation (add/sub/mul/div/matmul/pow/neg/sum/mean/relu/sigmoid/tanh/softmax, plus a fused `backward_cross_entropy` for softmax+CCE).
- **Known, intentional limitations of v0.1**: backward rules generally assume compatible shapes (no broadcasting) — this is documented, not a bug to silently "fix" by adding broadcasting logic unless asked. The one exception is `backward_add`, which *does* un-broadcast via the `unbroadcast(gradient, shape)` helper (sums a gradient back down to a parent's original shape when forward-pass broadcasting stretched it) — this exists specifically so `Linear`'s `x @ weight + bias` works with a batched input and a 1D bias. `backward_matmul`, `backward_multiply`, etc. still assume both operands already have compatible/equal shapes going in.
- `tensor.py` imports from `autograd.py` with a `try/except ImportError` fallback (relative import, then absolute `orbit.core.autograd`) — preserve this pattern if editing those imports, it's there to let files be run standalone via `python -m orbit.core.tensor` as well as imported as a package.

### The NN layer (`src/orbit/nn/`)

- **`parameter.py`** — `Parameter(Tensor)`, identical to `Tensor` except `requires_grad=True` by default. Marks "this tensor is a trainable weight."
- **`module.py`** — `Module` base class, PyTorch-inspired: `register_parameter`/`register_module` build `_params`/`_modules` dicts; `parameters()`/`named_parameters()`/`modules()`/`named_modules()` recurse through the tree; `forward()` must be overridden by subclasses; `__call__` dispatches to `forward()`; `zero_grad()`, `train()`/`eval()` propagate through submodules.
- **`layers/linear.py`** — `Linear(Module)`: `x @ weight + bias`. It uses `register_parameter` for both `weight` and `bias`, consistent with the `Module` registration pattern used elsewhere (e.g. in tests' `MLPDummy`), so both show up in `.parameters()`. `weight` is Xavier/Glorot-initialized (`scale = sqrt(2 / (in_features + out_features))` times `randn`) rather than raw `np.random.randn` — unscaled init lets a `Linear`'s output variance grow with `in_features`, which saturates `Tanh`/`Sigmoid` on wide layers (e.g. 120 units) and kills gradient flow before training starts; this was found and fixed after a wide (120-120-1) XOR model trained flat at loss≈0.25 (the exact MSE of a model stuck outputting ≈0.5 everywhere) for 10,000 epochs. `bias` stays zero-initialized (standard practice, no equivalent saturation risk).
- **`model.py`** — `Sequential(Module)`: `__init__(self, *layers)` registers each positional layer under its index (`register_module(str(i), layer)`); `forward` just loops `self._modules.values()` in order and threads `x` through each layer, relying on `dict` preserving insertion order (Python 3.7+) to keep that order correct. Lets simple stacks (e.g. `Sequential(Linear(2,8), Tanh(), Linear(8,1), Sigmoid())`) skip writing a one-off `Module` subclass.
- **`activations/`** — `Activation(Module)` base class + `ReLU`, `Sigmoid`, `Tanh`, `Softmax`, each a thin wrapper calling the corresponding `Tensor` method (`x.relu()`, etc.) and storing `self.output`.
- **`losses/`** — plain (non-`Module`) `Loss` base class with `forward(y_pred, y_true)`; `MSE` composes existing `Tensor` ops (`(y_pred - y_true) ** 2).mean()`) so it gets autograd for free; `CrossEntropy` instead computes softmax+NLL directly in NumPy and wires a hand-derived fused backward rule (`backward_cross_entropy`) for numerical stability — these are two different, both-valid strategies for adding a loss (compose existing autograd ops vs. hand-derive a fused gradient), pick whichever fits a new loss's numerical stability needs.
- Note: `layers/__init__.py` and `losses/__init__.py` are empty (no re-exports), unlike `activations/__init__.py` and `optimizers/__init__.py` which do export their public classes. Import `Linear`/`MSE`/`CrossEntropy` via their full submodule path (`orbit.nn.layers.linear`, `orbit.nn.losses.mse`, ...) until that's made consistent.

### The data pipeline (`src/orbit/core/dataset.py`, `src/orbit/core/dataloader.py`)

- **`dataset.py`** — `Dataset` is a plain interface (no `abc`, matching the rest of the codebase): `__len__`/`__getitem__`/`input_shape`/`output_shape` all raise `NotImplementedError` on the base class. Contract: `__getitem__` returns one `(x, y)` sample as raw numpy (or scalars), **not** wrapped in `Tensor` — batching/`Tensor`-wrapping is `DataLoader`'s job, not `Dataset`'s. `TensorDataset(Dataset)` is the one concrete implementation: wraps two arrays (accepts numpy, lists, or `Tensor`s — unwraps `.data` if given a `Tensor`), validates matching row counts (`ValueError` if not), indexes both in lockstep, and exposes `input_shape`/`output_shape` as plain ints (`self.X.shape[-1]`/`self.Y.shape[-1]`, the per-sample feature count) — used by `core/config.py`'s `build_model` to validate a model's first/last `Linear` layer against the dataset it'll actually run on. `TensorDataset` itself never rescales anything (its `__init__` only coerces `Tensor`/list inputs to `np.ndarray`); input normalization lives in the same file as `fit_normalizer`/`apply_normalizer`/`NormalizedDataset`, a wrapper view applied by `load_experiment` when a config sets `"normalize"` — see "Planned: normalization, train/test split..." above (item #1). `dataset.py` also has `Subset(Dataset)` (a generic index-based view delegating `__len__`/`__getitem__`/`input_shape`/`output_shape` to a wrapped `Dataset` plus an index list) and `train_test_split(dataset, test_split) -> (Subset, Subset)`, which splits via `np.random.permutation` (drawing from the same global RNG `DataLoader`'s shuffle already uses) and raises `ValueError` if either resulting split would be empty — backs the `"test_split"` config field, see "Planned: normalization, train/test split..." above (item #2). `DataLoader` itself is unchanged: it always iterates whichever `Dataset`/`Subset` it's given, with no split awareness of its own — `load_experiment` builds two separate `DataLoader`s (train, test) off the two `Subset`s when a split is configured.
- **`dataloader.py`** — `DataLoader(dataset, batch_size=32, shuffle=True)`. `__len__` is `math.ceil(len(dataset) / batch_size)`. `__iter__` is a **generator** (has to be, not a method that builds and returns a list) — that's what makes every fresh `for X_batch, Y_batch in dataloader:` loop (i.e. every epoch) reshuffle independently rather than replaying the same order forever. Per batch: gathers `dataset[i]` for each index in the chunk, `np.stack`s the x's and y's separately, wraps both as `Tensor` — this is the one place in the pipeline where raw numpy becomes a `Tensor`. The last batch of an epoch is naturally short when the dataset doesn't divide evenly by `batch_size`; no special-casing needed, plain slicing handles it.

### The training layer (`src/orbit/nn/optimizers/`, `src/orbit/nn/training/`)

- **`optimizers/optimizer.py`** — `Optimizer` base class: `__init__(self, parameters, lr=0.01)` stores the parameter list and learning rate; `step()` raises `NotImplementedError`; `zero_grad()` sets every parameter's `.grad` to `None`. `Module.zero_grad()` does the same thing independently — call whichever one you already have a handle on.
- **`optimizers/sgd.py`** — `SGD(Optimizer)`: `step()` does plain `param.data -= self.lr * param.grad` for every parameter whose `.grad` is not `None`, skipping the rest.
- **`training/trainer.py`** — `Trainer.fit(model, loss_fn, optimizer, dataloader, epochs, verbose=False, log_every=100, accuracy_fn=None)` consumes a `DataLoader`: per epoch it loops every batch doing `model.zero_grad()` → `loss.backward()` → `optimizer.step()` (zero_grad happens **per batch**, not per epoch, so gradients never leak across batches), and accumulates a `batch_size`-weighted running sum to compute a true epoch-average loss (a plain float/numpy scalar, not a `Tensor` — you can't return a single `Tensor` representing an epoch once several batches' graphs have each already been consumed by their own `.backward()` call). If `accuracy_fn` is given (a `(y_pred, y_true) -> float` callable, e.g. `core/metrics.py`'s `accuracy`/`accuracy_multiclass`, resolved from a config's `"task"` field via `core/config.py`'s `TASK_REGISTRY`/`build_accuracy_fn`), the same batch-weighted-average treatment is applied to accuracy alongside loss. The per-batch loop lives in a private `_run_epoch(model, loss_fn, optimizer, dataloader, accuracy_fn=None) -> (avg_loss, grad_norm, avg_accuracy)` helper, called once per epoch by `fit()` — it also computes `grad_norm` via the module-level `_gradient_norm(model)` helper (L2 norm of every parameter's gradient, flattened and concatenated, skipping parameters whose `.grad` is `None`) *after* `optimizer.step()` for that epoch's last batch, so it reports that last batch's gradient norm specifically, not a running average across the epoch — the standard signal for spotting vanishing/exploding gradients, not epoch-precise but cheap and directionally useful. `fit` returns the final epoch's average loss; `Trainer` also keeps three parallel per-epoch histories for anything that wants the full curves (e.g. `Experiment`/`Results` below): `self.history` (loss, always), `self.gradient_norm_history` (always), and `self.accuracy_history` (only initialized to a list, and only appended to, when `accuracy_fn is not None` — stays `None` otherwise, matching `Results.accuracy_history`'s optionality). `fit` also times the whole call with `time.time()` and stores the elapsed wall-clock seconds on `self.duration_seconds`. When `verbose=True` and stdout is a real terminal (`orbit.ui.is_tty()`), `fit()` renders a live `rich.progress.Progress` bar (percentage, epoch count, current loss, elapsed/remaining time) instead of printing — `log_every` is a no-op on that path since the bar updates every epoch. When output isn't a real terminal (piped, redirected to a file, or running under `pytest`), the original `print(f"{e} | {avg_loss}")` behavior (gated by `log_every`) is unchanged — this is why `tests/integration/test_xor.py`'s `verbose=True` run is unaffected. `Trainer.evaluate(model, loss_fn, dataloader, accuracy_fn=None) -> (loss, accuracy)` is the forward-pass-only counterpart to `fit()` — no `zero_grad()`/`backward()`/`optimizer.step()`, brackets the pass in `model.eval()`/`model.train()`, mirrors `_run_epoch`'s batch-weighted averaging. Built for `Experiment.run()`'s post-training test-set pass (see "Planned: normalization, train/test split..." above, item #2, now implemented) and reused as-is by the standalone `orbit test` command (item #3, also implemented). Like `fit()`, `evaluate()` renders a live `rich.progress.Progress` bar when stdout is a real terminal (`orbit.ui.is_tty()`) — via a private `_evaluate_with_progress_bar` helper mirroring `_fit_with_progress_bar`'s style (same `#ffeab0` "Evaluating" label, bar, elapsed/remaining time) — except gated on `is_tty()` alone rather than a `verbose` flag (`evaluate()` has no such parameter; every call wants the bar when interactive) and tracking *batches* rather than epochs, since a single evaluation pass has no epoch loop to report progress against; `evaluate()` has no non-progress-bar `print()` fallback the way `fit()` does, since it's only ever called after training already finished (nothing incremental worth logging to a non-tty stream). Both branches are functionally identical, just with/without the bar — `is_tty()` returning `False` under `pytest`'s `capsys` means existing tests exercise the plain loop unchanged.

### Experiment orchestration (`src/orbit/core/experiment.py`, `src/orbit/core/results.py`)

- **`results.py`** — `Results` is a plain, dependency-free data container (no imports from the rest of `orbit`, deliberately, since it's meant to be the thing a future `storage/` layer serializes): `name`, `final_loss`, `loss_history`, `hyperparams` (defaults to `{}`), plus `duration_seconds`, `gradient_norm_history`, `accuracy_history`, `test_loss`, and `test_accuracy` (all `Optional`, default `None` — `accuracy_history` stays `None` unless the experiment's config set a `"task"`, and `test_loss`/`test_accuracy` stay `None` unless it set `"test_split"`). `to_dict()`/`from_dict()` round-trip all of these through plain JSON-safe Python types (casting numpy scalars to `float`), including `None` for the optional fields, so `storage/experiments.py`'s save/load round trip already covers them with no extra work needed there.
- **`experiment.py`** — `Experiment(model, loss_fn, optimizer, dataloader, epochs, name=None, verbose=False, log_every=100, accuracy_fn=None, test_dataloader=None)` stores all of that plus its own `Trainer()` instance. `run(self)` (no extra args — everything needed is already on `self` from `__init__`) calls `self.trainer.fit(..., accuracy_fn=self.accuracy_fn)`, then, if `test_dataloader` was given, calls `self.trainer.evaluate(self.model, self.loss_fn, self.test_dataloader, accuracy_fn=self.accuracy_fn)` for a post-training test-set pass; both results are packaged into a `Results`, pulling `loss_history`/`gradient_norm_history`/`accuracy_history`/`duration_seconds` straight off the `Trainer` instance and building `hyperparams` from values already sitting on the objects it holds (`optimizer.lr`, `dataloader.batch_size`, `loss_fn.name`) rather than duplicating any of that state itself. `test_loss`/`test_accuracy` stay `None` when no `test_dataloader` is given — see "Planned: normalization, train/test split..." above (item #2) for how `core/config.py`'s `load_experiment` builds and passes one.

- **`metrics.py`** — plain functions, not classes (no autograd needed on final predictions): `accuracy(y_pred, y_true, threshold=0.5)` for binary classification, `accuracy_multiclass(y_pred, y_true)` using `np.argmax(y_pred.data, axis=-1)` vs. integer class labels. Both are now wired into `Trainer`/`Experiment`/`Results` — an experiment config's optional `"task"` field resolves through `core/config.py`'s `TASK_REGISTRY` to pick one of these as `Trainer.fit`'s `accuracy_fn`. No regression-style metric exists yet (a fixed-threshold/argmax match doesn't make sense for a continuous target like a wine-quality score) — see "Planned: normalization, train/test split..." above for the two regression task types on deck (`"regression_tolerance"`, `"regression_r2"`).

### Visualization (`src/orbit/visualization/`)

- **`loss.py`** — `plot_loss(results: Results, output_path: pathlib.Path, log_scale: bool = False) -> pathlib.Path`: renders `results.loss_history` as a `matplotlib` line plot (epoch on the x-axis, loss on the y-axis, `#b0ffb3` accent color, titled `f"{results.name} — Training Loss"`, `" (log scale)"` appended when `log_scale=True` alongside setting `ax.set_yscale("log")`) and saves it as a PNG at `output_path`, returning that path. Raises `ValueError` if `loss_history` is empty. `plot_loss_comparison(results_list: List[Results], output_path, log_scale=False) -> pathlib.Path` overlays several `Results`' loss curves on one plot instead of just one, each labeled by `results.name` via `ax.legend()`, using `matplotlib`'s default color cycle (not a fixed accent color, since a single fixed color can't distinguish multiple lines) — backs `orbit compare --plotloss` and `orbit plot`'s `loss` metric. Both import `matplotlib` lazily inside the function (not at module scope) and force the non-interactive `Agg` backend before importing `pyplot` — see `cli/commands/plotloss.py`'s doc note above for why.
- **`accuracy.py`**, **`gradients.py`** — same shape as `loss.py`, one function pair each: `plot_accuracy`/`plot_accuracy_comparison` (`results.accuracy_history`, y-axis "Accuracy", raw 0–1 fraction) and `plot_gradient_norm`/`plot_gradient_norm_comparison` (`results.gradient_norm_history`, y-axis "Gradient Norm"). Same `ValueError`-on-empty-history guard, same lazy `matplotlib` import.
- **`test_metrics.py`** — `plot_test_loss`/`plot_test_accuracy(results_list: List[Results], output_path, log_scale=False) -> pathlib.Path`: **bar charts**, not line plots, since `test_loss`/`test_accuracy` are single scalars per experiment, not per-epoch histories — one bar per `Results` in `results_list` (always a list, even for a single experiment, unlike `loss.py`'s single/comparison split — a bar chart already generalizes from 1 to N bars with the same code shape). Both delegate to a shared private `_bar_chart(...)` helper (`ax.bar(names, values, color="#b0ffb3")`, `fig.autofmt_xdate(rotation=30)` to keep experiment names legible). Raises `ValueError` on an empty list.
- `visualization/__init__.py` exports all of the above (`from .loss import ...`, `from .accuracy import ...`, etc.), matching the export convention every other `nn/*/__init__.py`-style package in this codebase already uses. `weights.py`/`terminal.py` are still empty stubs — nothing tracks weight history yet, and `terminal.py`'s ASCII/terminal-rendered-plot purpose (distinct from `matplotlib`-to-PNG) isn't part of any planned work yet.

### Everything else

`core/config.py`, `ui.py`, `cli/parser.py`, `cli/repl.py`, every file under `cli/commands/` (`init.py`, `new.py`, `run.py`, `train.py`, `test.py`, `list.py`, `inspect.py`, `delete.py`, `compare.py`, `reproduce.py`, `copy.py`, `rename.py`, `import_dataset.py`, `plotloss.py`, `plot.py`), `storage/experiments.py`, `storage/datasets.py`, `storage/artifacts.py`, and `visualization/loss.py`, `accuracy.py`, `gradients.py`, `test_metrics.py` (+ `visualization/__init__.py`) are all implemented (see "Current reality" above; `cli/__init__.py`/`cli/commands/__init__.py` are still empty, but that's just an unused package marker, not a stub feature). `storage/database.py`, `sweeps/`, `research/`, `simulation/`, and `visualization/weights.py`/`terminal.py` are still empty placeholder files reflecting the target package layout from the project vision. Treat their presence as a hint about where new code should eventually live, not as existing behavior. `simulation/` in particular has no design notes yet — don't assume its purpose, ask if a task touches it.

### Known gaps / test coverage

- `tests/nn/test_linear.py`, `tests/nn/test_losses.py` exist as 0-byte stub files — `Linear` and both losses currently have no dedicated unit tests (only indirect coverage via `test_xor.py`).
- Reproducibility (`tests/integration/test_reproducibility.py`) is proven at the pipeline level (seed `np.random.seed(...)` before constructing a model/dataloader → identical `Results`, survives a `storage/` save/load round-trip) but not at the "reconstruct a `Model` from a saved file" level — that needs architecture serialization, which doesn't exist yet.

## Conventions to follow

- Match the existing style: heavy docstring/comment explanations of the *math* in `core/` (why a gradient rule is what it is), sparser comments in straightforward `nn/` wrapper code.
- New `Tensor` operations follow the established pattern: build the output `Tensor` with `operation=` and `parents=` set, then attach `out._backward = lambda res=out, ...: backward_<op>(res, ...)`, and add the corresponding `backward_<op>` function in `autograd.py`.
- New losses/activations/layers follow the existing base-class shape (`Loss`, `Activation`, `Module`) in their respective `__init__.py`-exported package.
- Test philosophy (see `tests/core/test_autograd.py`): small, single-purpose tests, each with a comment deriving the expected gradient by hand before asserting `np.allclose(...)`. Follow this for new autograd rules — a numerical/hand-derived check, not just a smoke test.
- Don't add broadcasting, shape-inference magic, or other generalizations beyond what the current op needs unless asked — v0.1 intentionally assumes compatible shapes. `backward_add`'s `unbroadcast` is the sole, deliberate exception (needed for `Linear`'s bias term); it is not a precedent for generalizing other ops without being asked.
